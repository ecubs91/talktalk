<div class="container">
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <h2><%= @conversation.participants.map{|a| !(a.id==current_user.id) ? "#{a.first_name.capitalize}" : nil }.compact!.first %>님</h2>
      <br>
<%= render 'reply_message_form' %>
     
<%= render 'messages_from_conversations_receipt' %>

      <%= link_to '메세지함', conversations_path, class: "btn btn-primary" %>
    </div>
  </div>
</div>

