<div class="navbar navbar-default" role="navigation">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">  
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a href="<%= root_path %>">
        <div class="navbar-brand">
          <img src="" style="height:45px">
        </div>
      </a>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="navbar-collapse collapse">       

      <ul class="nav navbar-nav navbar-right">
        <% if current_user.present? %>
          <% if current_user.authority == "admin" %>
            <li><%= link_to "Notify Users", admin_notify_users_path %></li>
          <% end %>
        <% end %>
        <li><%= link_to "", new_tutor_profiles_path %></li>
        <li><%= link_to "",tutor_profiles_path %></li>
        
                         <% if user_signed_in? %>
                          <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"> <%= current_user.first_name %>님 <span class="caret"></span></a>
                            <ul class="dropdown-menu" role="menu">
                            <% if current_user.tutor_profile.present? %>
                              <li><%= link_to "내 프로필", tutor_profile_path(current_user.tutor_profile.id) %></li>
                              <li><%= link_to "후기 요청", invite_for_review_tutor_profiles_path %></li>
                            <% end %>
                              <!--<li><%= link_to "수업 메뉴", mytuition_path %></li>-->
                              <li><%= link_to conversations_path, data: { no_turbolink: true } do %> <i class="glyphicon glyphicon-envelope"></i> 메세지<% end %></li>
                              <li><%= link_to edit_user_registration_path, data: { no_turbolink: true } do %>
                              <i class="glyphicon glyphicon-wrench"></i> 계정관리
                               <% end %></li>
                              <li><%= link_to "로그아웃", destroy_user_session_path, method: :delete %></li>           
                            </ul>                            
                          </li>  
                           <% else %>
                          <li class="nounderline"><%= link_to "회원가입", new_user_registration_path %></li>
                          <li class="nounderline"></i><%= link_to "로그인", new_user_session_path %></li>
                        <% end %>  
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container -->
</div>  

<script type="text/javascript">
    $(document).ready(function () {
        $('.dropdown-toggle').dropdown();
    });
</script>